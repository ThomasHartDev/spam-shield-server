# Neon Postgres pooled URL
DATABASE_URL=postgres://user:pass@ep-xxx.neon.tech/spam_shield?sslmode=require

# Shared bearer token for /api/* (iOS app + ingest webhook).
# Generate with: openssl rand -hex 32
API_KEY=change-me-32-byte-hex
